# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Database Connection (SQLite default for zero-setup local dev)
DATABASE_URL="file:./dev.db"

# Admin API Key (For viewing inquiries & bookings securely)
ADMIN_API_KEY="zynova_admin_secret_dev_key"

# Email Configuration (Optional - emails will fallback to console logging if not configured)
EMAIL_SERVICE=""
EMAIL_HOST=""
EMAIL_PORT=587
EMAIL_USER=""
EMAIL_PASS=""
NOTIFICATION_EMAIL="team.zynova@gmail.com"
